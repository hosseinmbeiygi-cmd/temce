# رجیستر جامع کنترل‌ها و الزامات صندوق‌های بورسی تهران

> نسخه: ۱.۰ — پیوست سند `NAV_ARCHITECTURE_V5.md`
> دامنه: ۲۳ حوزه، **۷۰۵ قلم** شماره‌دار (کنترل، الزام، تست، سنجه).
> هر قلم باید به ماژول، جدول و تست متناظر در سند اصلی نگاشت شود.
> اعداد مقرراتی از «قواعد نسخه‌دار» می‌آیند، نه ثابت در کد.

---

## الف) داده، منابع و کیفیت — ۴۰ قلم

1. هر منبع داده شناسه یکتا، SLA و مالک مشخص دارد.
2. هر دریافت خام با checksum و زمان دریافت ذخیره می‌شود.
3. آرشیو خام WORM با Object Lock فعال است.
4. نگاشت هر فیلد منبع به مدل داخلی مستند است.
5. نسخه قرارداد هر API ثبت می‌شود.
6. سهمیه درخواست هر منبع پایش و محدود می‌شود.
7. Retry با Backoff نمایی و Jitter برای خطای گذرا.
8. خطای ساختاری بدون تکرار به قرنطینه می‌رود.
9. هر رکورد قرنطینه دلیل و وضعیت رفع دارد.
10. تازگی هر منبع (سن داده) محاسبه و هشدار داده می‌شود.
11. کنترل جهش قیمت نسبت به دامنه مجاز و تاریخچه.
12. رد قیمت صفر یا منفی.
13. رد یا علامت‌گذاری Crossed Book.
14. کنترل تیک خارج از اندازه تیک.
15. کنترل ترتیب زمانی تیک‌ها و رد عقب‌رفت.
16. کنترل تکراری‌بودن تیک با کلید طبیعی.
17. کنترل پوشش نمادها در برابر فهرست مرجع.
18. کنترل تطابق حجم تجمعی با مجموع تیک‌ها.
19. کنترل سازگاری OHLCV با تیک.
20. کنترل ریال/تومان در هر ورودی و تبدیل در مرز.
21. کنترل نرخ ارز در برابر منبع مرجع و بازه مجاز روزانه.
22. کنترل تازگی پرتفوی افشاشده (`portfolio_age_days`).
23. کنترل سهم قیمت مدل‌شده از کل ارزش پرتفوی.
24. Lineage هر عدد تا پاسخ خام قابل رهگیری است.
25. کنترل هم‌رسانی شناسه بین TSETMC/CSDI/IME.
26. تشخیص تغییر نماد و نگاشت تاریخچه.
27. تشخیص ابزار جدید و الزام ثبت در مرجع.
28. کنترل تاریخ‌های تقویم معاملاتی و تعطیلی.
29. ذخیره UTC و نمایش Asia/Tehran.
30. همگام‌سازی ساعت سرورها با NTP.
31. کنترل جهش NAV مرجع نسبت به روز قبل.
32. کنترل مرجع تکراری یا نسخه قدیمی‌تر.
33. کنترل کامل‌بودن اجزای گزارش API.
34. کنترل عدم دریافت داده پس از knowledge cutoff در snapshot.
35. کنترل تناقض بین منابع (قیمت پایانی در برابر تیک).
36. هشدار تأخیر فید بیش از آستانه.
37. گزارش روزانه کیفیت داده برای هر صندوق.
38. سابقه اصلاحات داده و بازتجمیع.
39. کنترل حذف‌نشدن داده تاریخی (append-only).
40. کنترل دسترسی نوشتن به آرشیو خام فقط سرویس ingestion.

## ب) مرجع ابزار و صندوق — ۳۵ قلم

41. هر ابزار شناسه پایدار UUID دارد.
42. هر شناسه منبع بازه اعتبار دارد.
43. قید عدم‌هم‌پوشانی شناسه در DB فعال است.
44. نوع ابزار دقیق (سهام، حق تقدم، اوراق، سپرده، گواهی، ETF، اختیار، آتی، ملک).
45. ضریب قرارداد هر ابزار ثبت و نسخه‌دار است.
46. ارز و واحد قیمت هر ابزار صریح است.
47. اندازه تیک و لات از قواعد بازار می‌آید.
48. سررسید و مشخصات کوپن برای اوراق ثبت است.
49. دارایی پایه برای مشتقه ثبت است.
50. نرخ اعمال و نوع اختیار برای آپشن ثبت است.
51. تاریخچه نام و نماد نگهداری می‌شود.
52. طبقه‌بندی صنعت با تاریخ اعتبار ثبت است.
53. وضعیت فعال/غیرفعال با بازه اعتبار ثبت است.
54. نگاشت به CSDI و IME و کدال ثبت است.
55. ابزار بدون نگاشت معتبر مسدود می‌شود.
56. صندوق با شخصیت حقوقی مستقل ثبت می‌شود.
57. طبقات واحد و نوع NAV هر طبقه ثبت است.
58. نسخه امیدنامه و منشور صندوق نگهداری می‌شود.
59. سیاست ارزش‌گذاری هر صندوق ثبت است.
60. کارمزدهای امیدنامه با تاریخ اعتبار ثبت است.
61. نصاب‌های انطباق هر صندوق ثبت است.
62. تقویم معاملاتی صندوق ثبت است.
63. حداقل سرمایه و تعداد مؤسسین ثبت است.
64. شرایط تمدید و عمر صندوق ثبت است.
65. ارکان (ضامن/بازارگردان/متولی) و تغییرات ثبت است.
66. هر تغییر پیکربندی صندوق نسخه‌بندی می‌شود.
67. استفاده از پیکربندی منقضی در محاسبه ممنوع است.
68. واحدهای ممتاز عام/خاص و حق رأی ثبت است.
69. محدودیت‌های نقل و انتقال واحد ثبت است.
70. مشخصات ضامن و کفایت سرمایه ثبت است.
71. قراردادهای ارکان و SLA ثبت است.
72. مجوزها و تاریخ اعتبارشان ثبت است.
73. انقضای مجوز ارکان پایش و هشدار می‌شود.
74. سابقه انتظامی ارکان ثبت است.
75. استقلال متولی و حسابرس کنترل می‌شود.

## ج) دفتر مالی و حسابداری — ۵۰ قلم

76. هر سند متوازن است (بدهکار = بستانکار).
77. قید علامت مبالغ در DB فعال است.
78. ثبت هم‌زمان بدهکار و بستانکار در یک خط ممنوع است.
79. هر سند شناسه idempotency یکتا دارد.
80. ثبت تکراری منبع خارجی ممنوع است.
81. سند برگشتی به سند اصلی لینک دارد.
82. حذف فیزیکی اسناد ممنوع و ابطال جایگزین آن است.
83. کدینگ حساب‌ها مرجع رسمی دارد.
84. حساب صندوق از مدیر و سایر صندوق‌ها مجزاست.
85. خرید در تاریخ معامله با پرداختنی تسویه ثبت می‌شود.
86. فروش با دریافتنی و سود/زیان تحقق‌یافته ثبت می‌شود.
87. تسویه جدا از معامله ثبت می‌شود.
88. بهای تمام‌شده طبق سیاست (میانگین/FIFO) محاسبه می‌شود.
89. سود/زیان تحقق‌یافته هر فروش محاسبه می‌شود.
90. سود تحقق‌یافته از تغییر ارزش تفکیک می‌شود.
91. تعهد روزشمار کارمزد مدیر ثبت می‌شود.
92. تعهد روزشمار کارمزد متولی ثبت می‌شود.
93. تعهد کارمزد حسابرس و مدیر ثبت ثبت می‌شود.
94. تعهد کارمزد ضامن و بازارگردان ثبت می‌شود.
95. تعهد کارمزد ثبت و نظارت سازمان ثبت می‌شود.
96. تعهد هزینه نرم‌افزار، تارنما و رتبه‌بندی ثبت می‌شود.
97. سود سپرده بانکی به روش تعهدی شناسایی می‌شود.
98. سود اوراق با Day-Count شناسایی می‌شود.
99. دوباره‌شماری سود در قیمت کثیف ممنوع است.
100. سود سهام در تاریخ تصویب/Ex ثبت می‌شود.
101. ذخیره مطالبات مشکوک‌الوصول ثبت می‌شود.
102. ذخیره کاهش ارزش سرمایه‌گذاری غیربورسی ثبت می‌شود.
103. اندوخته غیرقابل تقسیم طبق استاندارد ۱۵ ثبت می‌شود.
104. سود تقسیمی صندوق و بدهی واحدداران ثبت می‌شود.
105. نقد/سپرده در موقعیت و تراز دوباره جمع نمی‌شود.
106. وجه تضمین و تفکیک مسدود ثبت می‌شود.
107. Variation Margin روزانه مشتقه ثبت می‌شود.
108. تعهدات احتمالی تضمین ثبت می‌شود.
109. استقراض و سود روزشمار آن ثبت می‌شود.
110. هزینه دعاوی و رویدادهای غیرمترقبه ثبت می‌شود.
111. گردکردن فقط در نقاط پایانی سیاست انجام می‌شود.
112. سیاست گردکردن مستند است.
113. دوره مالی و بستن حساب‌ها کنترل می‌شود.
114. مانده افتتاحیه با منبع و تأیید ثبت می‌شود.
115. توازن تراز آزمایشی روزانه کنترل می‌شود.
116. نقد روزانه با بانک تطبیق می‌شود.
117. نقد روزانه با کارگزار تطبیق می‌شود.
118. موقعیت با CSDI/متولی تطبیق می‌شود.
119. واحدها با مدیر ثبت تطبیق می‌شود.
120. پرداخت دومرحله‌ای (مدیر + متولی) است.
121. امضای مجاز پرداخت کنترل می‌شود.
122. هر تغییر با کاربر/زمان/دلیل ثبت می‌شود.
123. ویرایش مستقیم دفاتر ممنوع است.
124. توالی و شماره‌گذاری اسناد کنترل می‌شود.
125. گزارش روزانه سود/زیان صندوق تولید می‌شود.

## د) ارزش‌گذاری — ۴۰ قلم

126. هر دارایی Rule ارزش‌گذاری نسخه‌دار دارد.
127. قیمت انتخابی از منبع معتبر و زمان‌دار است.
128. سیاست انتخاب قیمت هر نوع دارایی مستند است.
129. قیمت پایانی سهام طبق قواعد بازار است.
130. قاعده صریح نماد متوقف وجود دارد.
131. قاعده صریح صف خرید/فروش وجود دارد.
132. تعدیل قیمت پس از رویداد شرکتی اعمال می‌شود.
133. حق تقدم جدا از سهم مادر ارزش‌گذاری می‌شود.
134. اوراق تمیز/کثیف طبق سیاست ارزش‌گذاری می‌شود.
135. سود متعلقه اوراق محاسبه می‌شود.
136. سپرده با اصل + سود تعهدی ارزش‌گذاری می‌شود.
137. جریمه شکست سپرده اعمال می‌شود.
138. گواهی کالا با ضریب ارزش‌گذاری می‌شود.
139. حباب گواهی از دارایی پایه تفکیک می‌شود.
140. ETF با قیمت تابلو (حسابداری) ارزش‌گذاری می‌شود.
141. Look-through فقط در تحلیل استفاده می‌شود.
142. FOF با NAV نسخه‌دار زیرصندوق ارزش‌گذاری می‌شود.
143. تازگی NAV زیرصندوق کنترل می‌شود.
144. حلقه حلقوی بین صندوق‌ها کشف می‌شود.
145. اختیار با بازار یا مدل مصوب ارزش‌گذاری می‌شود.
146. مدل ارزش‌گذاری نسخه‌بندی می‌شود.
147. مدل با داده بازار اعتبارسنجی می‌شود.
148. آتی با MTM روزانه ارزش‌گذاری می‌شود.
149. ارزش اسمی آتی وارد NAV نمی‌شود.
150. املاک با ارزیاب رسمی ارزش‌گذاری می‌شود.
151. پرونده ارزیابی و نسخه آن ثبت می‌شود.
152. پروژه با درصد تکمیل ارزش‌گذاری می‌شود.
153. شرکت غیربورسی با ارزش‌گذاری مصوب کمیته ارزش‌گذاری می‌شود.
154. دارایی ارزی با نرخ مرجع ارزش‌گذاری می‌شود.
155. تجدید ارزیابی ارزی و تفاوت آن ثبت می‌شود.
156. مطالبات با احتمال وصول ارزش‌گذاری می‌شود.
157. بدهی‌ها به مبلغ پرداختنی ارزش‌گذاری می‌شود.
158. اقلام در جمع‌ها دوباره‌شماری نمی‌شوند.
159. خروجی ردیفی ارزش‌گذاری با منشأ تولید می‌شود.
160. کیفیت داده هر ردیف ثبت می‌شود.
161. شناسه مدارک هر ردیف ثبت می‌شود.
162. گردکردن ارزش‌ها طبق سیاست انجام می‌شود.
163. تغییرات غیرعادی ارزش روزانه کنترل می‌شود.
164. موارد خاص به کمیته ارزش‌گذاری ارجاع می‌شود.
165. مصوبات کمیته ارزش‌گذاری نسخه‌دار است.

## هـ) NAV — ۳۵ قلم

166. فرمول NAV طبق سند و واحد محاسبه می‌شود.
167. NAV آماری/صدور/ابطال/پایانی تفکیک می‌شود.
168. NAV هر طبقه جداگانه محاسبه می‌شود.
169. الگوریتم اهرمی کف/سقف روزانه اجرا می‌شود.
170. انتقال روزانه ارزش بین طبقات ثبت می‌شود.
171. هزینه بهره اهرم روزانه محاسبه می‌شود.
172. سقف ۲ برابری عادی/ممتاز کنترل می‌شود.
173. نرخ کف/سقف نسخه‌دار ثبت می‌شود.
174. تغییر نرخ یک ماه قبل اعلام می‌شود.
175. دوره نگهداری تضمین کنترل می‌شود.
176. جبران FIFO هنگام ابطال محاسبه می‌شود.
177. ذخیره کارمزد ضامن نگهداری می‌شود.
178. واریز تکمیلی و جریمه تأخیر کنترل می‌شود.
179. تعهد احتمالی تضمین جدا از NAV ثبت می‌شود.
180. NAV از NAV فراصندوق محاسبه می‌شود.
181. سقف‌های FOF کنترل می‌شود.
182. NAV تخمینی با عدم‌قطعیت اعلام می‌شود.
183. ارزش نقدشوندگی/سناریوی خروج محاسبه می‌شود.
184. Mid/آخرین بدون سیاست استفاده نمی‌شود.
185. قرارداد بازتولید اجرا کامل است.
186. input_hash با canonicalization محاسبه می‌شود.
187. ترتیب محاسبه قطعی است.
188. ledger_snapshot_id و price_snapshot_id ثبت می‌شود.
189. policy_version و engine_version ثبت می‌شود.
190. quality_status از run_status جدا است.
191. NAV قبل از انتشار تأیید می‌شود.
192. NAV ناقص به‌عنوان دقیق منتشر نمی‌شود.
193. رقیق‌سازی صدور/ابطال محاسبه می‌شود.
194. گردکردن NAV واحد طبق سیاست است.
195. NAV منفی/صفر کنترل می‌شود.
196. جهش NAV نسبت به روز قبل کنترل می‌شود.
197. NAV با شاخص و گروه همتا مقایسه می‌شود.
198. NAV تاریخی بازتولیدپذیر است.
199. کامل‌بودن موقعیت‌ها در snapshot کنترل می‌شود.
200. تطابق تعداد واحدها با مدیر ثبت کنترل می‌شود.

## و) تطبیق — ۳۰ قلم

201. گیت یکسان‌بودن صندوق اعمال می‌شود.
202. گیت یکسان‌بودن طبقه واحد اعمال می‌شود.
203. گیت نوع NAV اعمال می‌شود.
204. گیت ارز/واحد اعمال می‌شود.
205. گیت زمان ارزش‌گذاری اعمال می‌شود.
206. گیت knowledge cutoff اعمال می‌شود.
207. گیت مبنای قیمت اعمال می‌شود.
208. گیت نقطه زمانی واحدها اعمال می‌شود.
209. گیت کامل‌بودن داخلی اعمال می‌شود.
210. گیت اعتبار و تازگی مرجع اعمال می‌شود.
211. NAV مرجع ≤ ۰ نامعتبر اعلام می‌شود.
212. اختلاف مطلق محاسبه می‌شود.
213. اختلاف bps محاسبه می‌شود.
214. آستانه دوگانه اعمال می‌شود.
215. نسخه آستانه ثبت می‌شود.
216. آستانه از توزیع تاریخی کالیبره می‌شود.
217. سه بُعد وضعیت جدا ثبت می‌شود.
218. probable_cause و residual جدا ثبت می‌شود.
219. چرخه عمر Break مدیریت می‌شود.
220. SLA و مالک Break تعیین می‌شود.
221. شواهد و پیوست Break ثبت می‌شود.
222. بازتطبیق پس از restatement اجرا می‌شود.
223. گزارش مرجع نسخه‌بندی می‌شود.
224. نقد با بانک تطبیق می‌شود.
225. نقد با کارگزار تطبیق می‌شود.
226. موقعیت با CSDI تطبیق می‌شود.
227. واحد با مدیر ثبت تطبیق می‌شود.
228. پرتفوی افشاشده کدال با دفتر تطبیق می‌شود.
229. drift پرتفوی تشخیص داده می‌شود.
230. گزارش روزانه وضعیت تطبیق تولید می‌شود.

## ز) واحدها و صدور/ابطال — ۳۵ قلم

231. دفتر واحدداران کامل است.
232. صدور با NAV صدور ثبت می‌شود.
233. ابطال با NAV ابطال ثبت می‌شود.
234. محاسبه روز بعد درخواست طبق رویه است.
235. کارمزد صدور ثابت/متغیر محاسبه می‌شود.
236. کارمزد ابطال پله‌ای محاسبه می‌شود.
237. تخفیف کارمزد اعمال می‌شود.
238. حداقل بسته صدور/ابطال ETF کنترل می‌شود.
239. تسویه نقدی/فیزیکی ETF ثبت می‌شود.
240. واحدها در CSDI ثبت می‌شود.
241. تطبیق روزانه با CSDI انجام می‌شود.
242. توثیق/خروج از وثیقه واحد ثبت می‌شود.
243. انتقال واحد و مالکیت ثبت می‌شود.
244. ارث و انحصار وراثت ثبت می‌شود.
245. مسدودی و رفع مسدودی ثبت می‌شود.
246. تغییر طبقه و نسبت تبدیل ثبت می‌شود.
247. سود نقدی و تاریخ استحقاق ثبت می‌شود.
248. پرداخت سود و تطبیق واریز انجام می‌شود.
249. سود در NAV و درآمد دوباره شمرده نمی‌شود.
250. پنجره ابطال/قفل کنترل می‌شود.
251. تعلیق ابطال و مدیریت فشار اعمال می‌شود.
252. صف درخواست‌ها و اولویت مدیریت می‌شود.
253. واحد اعشاری و گردکردن طبق سیاست است.
254. تعداد واحد منفی کنترل می‌شود.
255. مجموع واحدها با NAV تطبیق می‌شود.
256. جریان ورود/خروج پول گزارش می‌شود.
257. جریان حقیقی/حقوقی تحلیل می‌شود.
258. تراکنش مشکوک صدور/ابطال پایش می‌شود.
259. سجام/احراز هویت واحددار کنترل می‌شود.
260. طبقه‌بندی سرمایه‌گذار حرفه‌ای/غیرحرفه‌ای ثبت است.
261. بیانیه پذیرش ریسک اخذ می‌شود.
262. محدودیت سرمایه‌گذاری هر واحددار کنترل می‌شود.
263. ترکیب واحدداران گزارش می‌شود.
264. تمرکز مالکان هشدار داده می‌شود.
265. سابقه تغییرات دفتر واحدها نگهداری می‌شود.

## ح) رویدادهای شرکتی — ۲۵ قلم

266. تقویم رویدادهای شرکتی نگهداری می‌شود.
267. سود نقدی و Ex-Date ثبت می‌شود.
268. دریافتنی سود ثبت می‌شود.
269. وصول سود پیگیری می‌شود.
270. افزایش سرمایه از آورده ثبت می‌شود.
271. حق تقدم و مهلت استفاده ثبت می‌شود.
272. فروش حق استفاده‌نشده ثبت می‌شود.
273. افزایش از سود انباشته ثبت می‌شود.
274. تجدید ارزیابی بدون اثر NAV کنترل می‌شود.
275. کاهش سرمایه ثبت می‌شود.
276. تغییر نماد/شناسه نگاشت می‌شود.
277. ادغام/تفکیک نماد اعمال می‌شود.
278. تقسیم سود صندوق ثبت می‌شود.
279. تبدیل گواهی به کالا ثبت می‌شود.
280. تعدیل قیمت پایه پس از رویداد اعمال می‌شود.
281. اعمال رویداد نسخه‌دار است.
282. رویداد تکراری کنترل می‌شود.
283. رویداد معلق/نامشخص مدیریت می‌شود.
284. اثر رویداد بر NAV گزارش می‌شود.
285. ابزار بدون رویداد اعمال‌شده شناسایی می‌شود.
286. رویداد با CSDI/کدال همگام می‌شود.
287. منبع اطلاع رویداد ثبت می‌شود.
288. رویداد قبل از اعمال تأیید می‌شود.
289. سابقه اصلاح رویداد نگهداری می‌شود.
290. تأخیر اعمال رویداد کنترل می‌شود.

## ط) انطباق و نصاب — ۳۵ قلم

291. نصاب ترکیب دارایی هر نوع صندوق کنترل می‌شود.
292. حداقل سهام صندوق اهرمی کنترل می‌شود.
293. سقف ناشر کنترل می‌شود.
294. سقف صنعت کنترل می‌شود.
295. سقف واحد صندوق‌های دیگر (FOF) کنترل می‌شود.
296. سقف صندوق‌های هم‌مدیر کنترل می‌شود.
297. سقف سپرده FOF کنترل می‌شود.
298. نسبت اوراق دولتی درآمد ثابت کنترل می‌شود.
299. محدودیت مشتقه کنترل می‌شود.
300. فعالیت خارج از مجوز ممنوع است.
301. معاملات با اشخاص وابسته کنترل می‌شود.
302. کنترل پیش‌معامله فعال است.
303. کنترل پس‌معامله فعال است.
304. Breach Register نگهداری می‌شود.
305. چرخه رفع نقض مدیریت می‌شود.
306. نقض به سازمان گزارش می‌شود.
307. نصاب در سطح صندوق و طبقه کنترل می‌شود.
308. نصاب روزانه کنترل می‌شود.
309. نصاب لحظه‌ای کنترل می‌شود.
310. نزدیک‌شدن به نصاب هشدار داده می‌شود.
311. نسخه قواعد انطباق ثبت می‌شود.
312. قواعد منقضی استفاده نمی‌شود.
313. سرمایه‌گذاری در صندوق خودی ممنوع است.
314. معاملات متقابل کنترل می‌شود.
315. عرضه اولیه و تخصیص کنترل می‌شود.
316. سقف سفارش کنترل می‌شود.
317. حد نصاب نقد کنترل می‌شود.
318. حداقل دارایی کنترل می‌شود.
319. سقف استقراض کنترل می‌شود.
320. وثیقه‌گذاری مجاز کنترل می‌شود.
321. ممنوعیت تضمین سود در تبلیغات کنترل می‌شود.
322. عبارات ممنوع تبلیغاتی کنترل می‌شود.
323. درج ریسک در تبلیغات کنترل می‌شود.
324. پذیره‌نویسی و قفل حساب کنترل می‌شود.
325. لغو مجوز پذیره‌نویسی و انحلال کنترل می‌شود.

## ی) AML/CFT — ۳۰ قلم

326. شناسایی مشتری و ذی‌نفع واقعی انجام می‌شود.
327. طبقه‌بندی ریسک مشتری انجام می‌شود.
328. سجام و احراز هویت کنترل می‌شود.
329. فهرست‌های ابلاغی تطبیق می‌شود.
330. PEP و اشخاص مظنون پایش می‌شود.
331. تراکنش‌ها پایش می‌شود.
332. سناریوهای کشف کمی‌سازی می‌شود.
333. هشدار کم/متوسط/پرریسک طبقه‌بندی می‌شود.
334. گزارش معاملات مشکوک تا پایان روز کاری ارسال می‌شود.
335. گزارش حد نصاب ارسال می‌شود.
336. گزارش نقدی تجمیعی هفتگی ارسال می‌شود.
337. عدم اطلاع‌رسانی به ارباب‌رجوع کنترل می‌شود.
338. وضعیت گزارش‌شده از دید مشتری عایق‌بندی می‌شود.
339. صف استعلامات مرکز و پاسخ یک‌روزه مدیریت می‌شود.
340. تلاش‌های ناموفق/منصرف ثبت می‌شود.
341. گزارش کارمند (ماده ۱۳۹) ثبت می‌شود.
342. واحد AML مستقل و دسترسی برخط دارد.
343. تأییدیه سامانه AML پیش از بهره‌برداری اخذ می‌شود.
344. اسناد AML نگهداری می‌شود.
345. امحای اسناد طبق مقررات انجام می‌شود.
346. محرمانگی گزارش‌ها حفظ می‌شود.
347. آموزش AML برگزار می‌شود.
348. بازرسی داخلی AML انجام می‌شود.
349. گزارش دوره‌ای AML به مرکز ارسال می‌شود.
350. انطباق با الزامات سطح مؤسسه کنترل می‌شود.
351. سابقه رسیدگی به هشدارها نگهداری می‌شود.
352. تراکنش نقدی بالای آستانه کنترل می‌شود.
353. تجمیع تراکنش‌ها در پنجره زمانی محاسبه می‌شود.
354. آستانه‌ها پارامتریک هستند.
355. اثربخشی سامانه AML گزارش می‌شود.

## ک) مالیات — ۳۰ قلم

356. معافیت تبصره ۱ ماده ۱۴۳ مکرر اعمال می‌شود.
357. مالیات مقطوع ۰.۵٪ فروش سهام محاسبه می‌شود.
358. معافیت بازارگردان (تبصره ۵) اعمال می‌شود.
359. وضعیت مالیات واحدهای صندوق تعیین می‌شود.
360. CGT و معافیت بورس اعمال می‌شود.
361. مالیات سود سپرده کنترل می‌شود.
362. مالیات سود نقدی شرکت‌ها کنترل می‌شود.
363. مالیات گواهی سپرده کالایی کنترل می‌شود.
364. معافیت صندوق املاک (جهش تولید مسکن) اعمال می‌شود.
365. مالیات ادغام محاسبه می‌شود.
366. مالیات انحلال محاسبه می‌شود.
367. مالیات اجاره املاک صندوق محاسبه می‌شود.
368. مالیات ارزش افزوده کارمزدها کنترل می‌شود.
369. مالیات حقوق کارکنان کنترل می‌شود.
370. مالیات تکلیفی پیمانکاران کنترل می‌شود.
371. اظهارنامه و مهلت‌ها مدیریت می‌شود.
372. گزارش‌دهی مالیاتی نهاد مالی انجام می‌شود.
373. کسر و وصول کارگزار و فهرست ۱۰ روزه کنترل می‌شود.
374. درآمد مشمول/معاف تفکیک می‌شود.
375. معافیت‌ها با نسخه ثبت می‌شود.
376. تغییر قوانین مالیاتی پایش می‌شود.
377. مالیات در NAV و سود لحاظ می‌شود.
378. گزارش مالیاتی سالانه تهیه می‌شود.
379. همکاری با حسابرس مالیاتی انجام می‌شود.
380. اسناد مالیاتی بایگانی می‌شود.
381. دوباره‌شماری معافیت کنترل می‌شود.
382. اثر مالیات بر جریان نقدی تحلیل می‌شود.
383. بدهی مالیاتی احتمالی ثبت می‌شود.
384. مالیات معاملات خارج از بورس کنترل می‌شود.
385. گزارش CGT واحدداران (در صورت تکلیف) تهیه می‌شود.

## ل) حاکمیت — ۳۰ قلم

386. ارکان و مصوبات ثبت می‌شود.
387. مجمع عمومی عادی/فوق‌العاده برگزار می‌شود.
388. مجمع امیدنامه‌ای برگزار و ثبت می‌شود.
389. کمیته حسابرسی فعال است.
390. کمیته ریسک فعال است.
391. کمیته انتصابات فعال است.
392. حسابرسی داخلی مستقل است.
393. گزارش سه‌ماهه حسابرسی داخلی ارائه می‌شود.
394. گزارش سالانه کنترل‌های داخلی تهیه می‌شود.
395. حسابرس درباره کنترل داخلی اظهارنظر می‌کند.
396. معاملات وابسته تصویب می‌شود.
397. معاملات وابسته افشا می‌شود.
398. منشور اخلاقی مصوب و اجرا می‌شود.
399. سوت‌زنی و حمایت از گزارش‌دهنده فعال است.
400. اطلاعات نهانی و لیست‌ها کنترل می‌شود.
401. معاملات شخصی کارکنان پیش‌تأیید می‌شود.
402. گزارش پایداری ESG تهیه می‌شود.
403. گزارش تفسیری مدیریت تهیه می‌شود.
404. شرایط ترهین/توثیق کنترل می‌شود.
405. صورت‌های مالی تأیید می‌شود.
406. انتخاب و تغییر حسابرس مدیریت می‌شود.
407. استقلال حسابرس و متولی کنترل می‌شود.
408. چرخه تصدی حسابرس کنترل می‌شود.
409. صورتجلسات و مصوبات بایگانی می‌شود.
410. اجرای مصوبات پیگیری می‌شود.
411. عملکرد هیئت‌مدیره ارزیابی می‌شود.
412. آموزش مستمر مدیران انجام می‌شود.
413. جانشین‌پروری و سرپرست موقت مدیریت می‌شود.
414. تضاد منافع مدیران ثبت می‌شود.
415. گزارش حاکمیتی سالانه تهیه می‌شود.

## م) تعارض منافع — ۲۵ قلم

416. تخصیص عادلانه بین پرتفوها کنترل می‌شود.
417. Best Execution مستند می‌شود.
418. تخصیص عرضه اولیه کنترل می‌شود.
419. معاملات عمده و بلوکی کنترل می‌شود.
420. معاملات معکوس پایش می‌شود.
421. گزارش دوره‌ای تخصیص تولید می‌شود.
422. دیوار آتش اطلاعاتی برقرار است.
423. لیست محدودیت/تماشا نگهداری می‌شود.
424. دوره نگهداری حداقلی کارکنان اعمال می‌شود.
425. پیش‌تأیید معاملات کارکنان اجرا می‌شود.
426. حساب‌های مرتبط کارکنان کنترل می‌شود.
427. هدیه/مزایای نامتعارف ممنوع است.
428. منافع مدیران افشا می‌شود.
429. معاملات سمت کنترل می‌شود.
430. front-running کنترل می‌شود.
431. معاملات هم‌زمان صندوق و سبد اختصاصی کنترل می‌شود.
432. رویه معاملات متقابل صندوق‌ها وجود دارد.
433. قیمت‌گذاری منصفانه معاملات متقابل کنترل می‌شود.
434. معاملات وابسته تأیید مستقل می‌شود.
435. نقض تعارض منافع گزارش می‌شود.
436. آموزش تعارض منافع برگزار می‌شود.
437. اطلاعات محرمانه سفارش کنترل می‌شود.
438. تحقیق و معامله تفکیک می‌شود.
439. دسترسی به سفارش‌های بزرگ کنترل می‌شود.
440. سابقه تصمیمات سرمایه‌گذاری نگهداری می‌شود.

## ن) حقوقی و انتظامی — ۲۵ قلم

441. صلاحیت حرفه‌ای مدیران احراز می‌شود.
442. تأییدیه کمیته صلاحیت اخذ می‌شود.
443. سابقه انتظامی ارکان نگهداری می‌شود.
444. تخلفات رسیدگی می‌شود.
445. تخلف به سازمان گزارش می‌شود.
446. شکایات در سمتا ثبت و پیگیری می‌شود.
447. کمیته سازش در دسترس است.
448. هیئت داوری در دسترس است.
449. کد رهگیری و مهلت‌ها مدیریت می‌شود.
450. دعاوی و احکام ثبت می‌شود.
451. بیمه مسئولیت D&O بررسی می‌شود.
452. مسئولیت مدنی مؤسسین مدیریت می‌شود.
453. انطباق قراردادها کنترل می‌شود.
454. حق رأی و حقوق مالکانه اعمال می‌شود.
455. نماینده واحدداران در صورت نیاز تعیین می‌شود.
456. اسناد و بایگانی حقوقی نگهداری می‌شود.
457. دوره نگهداری اسناد رعایت می‌شود.
458. امحای اسناد طبق رویه انجام می‌شود.
459. پاسخ به استعلام مراجع مدیریت می‌شود.
460. رخدادهای حقوقی گزارش می‌شود.
461. ریسک حقوقی پایش می‌شود.
462. وکالت و نمایندگی کنترل می‌شود.
463. تعهدات قراردادی ارکان پایش می‌شود.
464. مطالبات حقوقی پیگیری می‌شود.
465. پرونده‌های باز گزارش می‌شود.

## س) ریسک — ۳۰ قلم

466. مواجهه صنعتی محاسبه می‌شود.
467. تمرکز Top5/10 و HHI محاسبه می‌شود.
468. نقدشوندگی موقعیت محاسبه می‌شود.
469. سناریوی خروج اجرا می‌شود.
470. پریمیوم/دیسکانت محاسبه می‌شود.
471. اثر قیمت/معامله/درآمد تفکیک می‌شود.
472. بتا و همبستگی محاسبه می‌شود.
473. VaR و CVaR محاسبه می‌شود.
474. تست استرس اجرا می‌شود.
475. سناریوی ابطال انبوه بررسی می‌شود.
476. ریسک اعتباری اوراق پایش می‌شود.
477. رتبه‌بندی اوراق ثبت می‌شود.
478. نکول و بازسازی بدهی مدیریت می‌شود.
479. ریسک نرخ بهره (Duration) محاسبه می‌شود.
480. ریسک ارزی محاسبه می‌شود.
481. پوشش ریسک ارزی بررسی می‌شود.
482. ریسک عملیاتی شناسایی می‌شود.
483. ریسک انطباق پایش می‌شود.
484. ریسک شهرت مدیریت می‌شود.
485. ریسک حقوقی پایش می‌شود.
486. تمرکز واحددار پایش می‌شود.
487. ریسک نقدشوندگی صندوق مدیریت می‌شود.
488. محدودیت‌ها و سقف‌های ریسک تعیین می‌شود.
489. گزارش روزانه ریسک تولید می‌شود.
490. داشبورد ریسک ارائه می‌شود.
491. مدل‌های ریسک کالیبره می‌شود.
492. Backtesting مدل‌ها انجام می‌شود.
493. گزارش ریسک به کمیته ارائه می‌شود.
494. نقض سقف‌های ریسک ثبت می‌شود.
495. تمرکز صنعت/ناشر پایش می‌شود.

## ع) عملکرد و رتبه‌بندی — ۲۰ قلم

496. TWR محاسبه می‌شود.
497. MWR/IRR محاسبه می‌شود.
498. بازدهی سالانه‌شده محاسبه می‌شود.
499. Rolling Return محاسبه می‌شود.
500. شارپ محاسبه می‌شود.
501. سورتینو محاسبه می‌شود.
502. ترینر محاسبه می‌شود.
503. امگا محاسبه می‌شود.
504. M2 محاسبه می‌شود.
505. آلفا/بتا محاسبه می‌شود.
506. نسبت اطلاعات محاسبه می‌شود.
507. Tracking Error محاسبه می‌شود.
508. Active Share محاسبه می‌شود.
509. درجه‌بندی ستاره‌ای انجام می‌شود.
510. رتبه‌بندی کیفیت مدیریت انجام می‌شود.
511. تعدیل تمرکز دارایی اعمال می‌شود.
512. تعدیل نقدشوندگی اعمال می‌شود.
513. تعدیل تمرکز مالکان اعمال می‌شود.
514. مقایسه گروه همتا انجام می‌شود.
515. گزارش عملکرد دوره‌ای منتشر می‌شود.

## ف) گزارش و افشا — ۳۰ قلم

516. پرتفوی ماهانه تا دهم ماه بعد منتشر می‌شود.
517. صورت‌های مالی فصلی/سالانه تهیه می‌شود.
518. NAV روزانه به سنا/صندوق اعلام می‌شود.
519. فایل اطلاعات روزانه سازمان ارسال می‌شود.
520. اطلاعیه گروه الف/ب منتشر می‌شود.
521. تغییر نرخ سود یک ماه قبل افشا می‌شود.
522. تغییر کارمزد و ثبت امیدنامه انجام می‌شود.
523. تغییر مدیر/متولی افشا می‌شود.
524. گزارش متولی تهیه می‌شود.
525. گزارش حسابرس تهیه می‌شود.
526. گزارش تفسیری مدیریت تهیه می‌شود.
527. گزارش پایداری تهیه می‌شود.
528. گزارش کنترل‌های داخلی تهیه می‌شود.
529. گزارش ترکیب سهامداران (برنامه هفتم) تهیه می‌شود.
530. معاملات وابسته افشا می‌شود.
531. ارزش منصفانه افشا می‌شود.
532. صورت جریان نقدی تهیه می‌شود.
533. ریسک مالی افشا می‌شود.
534. تعهدات احتمالی افشا می‌شود.
535. معافیت‌های مالیاتی افشا می‌شود.
536. رویدادهای پس از تاریخ ترازنامه افشا می‌شود.
537. صحت و کامل‌بودن افشا کنترل می‌شود.
538. مهلت‌های افشا کنترل می‌شود.
539. گزارش‌های منتشرشده آرشیو می‌شود.
540. گزارش‌ها نسخه‌بندی می‌شود.
541. افشای اطلاعات نهانی ممنوع است.
542. هماهنگی با تارنمای صندوق انجام می‌شود.
543. تارنما به‌روز نگهداری می‌شود.
544. گزارش سالانه به سرمایه‌گذاران ارائه می‌شود.
545. آموزش سرمایه‌گذار ارائه می‌شود.

## ص) رویداد و تاب‌آوری — ۲۵ قلم

546. Outbox هم‌تراکنش با commit است.
547. تحویل at-least-once فرض می‌شود.
548. idempotency_key و قید یکتا فعال است.
549. Partition بر fund_id اعمال می‌شود.
550. ترتیب رویدادهای صندوق کنترل می‌شود.
551. داده دیررس نسخه اصلاحی می‌سازد.
552. Backoff و Jitter اعمال می‌شود.
553. DLQ برای خطای ساختاری فعال است.
554. NAD و بازگشت پیام مدیریت می‌شود.
555. Replay پشتیبانی می‌شود.
556. Shadow Accounting اجرا می‌شود.
557. Redis Streams و PEL استفاده می‌شود.
558. قفل توزیع‌شده امن است.
559. حذف پیام قبل از ثبت نتیجه ممنوع است.
560. allkeys-lru برای داده حیاتی ممنوع است.
561. بازیابی پیام درحال‌پردازش انجام می‌شود.
562. کنترل تکرار در مصرف‌کننده اعمال می‌شود.
563. Event schema نسخه‌بندی می‌شود.
564. سابقه انتشار و مصرف نگهداری می‌شود.
565. backlog صف پایش می‌شود.
566. تأخیر پردازش هشدار داده می‌شود.
567. بازیابی از پشتیبان انجام می‌شود.
568. DR دوره‌ای آزمون می‌شود.
569. RPO/RTO تعریف و پایش می‌شود.
570. BCP تدوین و آزمون می‌شود.

## ق) زیرساخت و امنیت — ۳۰ قلم

571. نسخه PostgreSQL/TimescaleDB/Redis ثابت است.
572. استفاده از latest ممنوع است.
573. جداول حجیم پارتیشن‌بندی می‌شود.
574. ایندکس FKها ساخته می‌شود.
575. قیود و CHECKها فعال است.
576. NUMERIC با دقت دامنه‌ای تعریف می‌شود.
577. پشتیبان چندلایه تهیه می‌شود.
578. بازیابی آزمون می‌شود.
579. WORM و Object Lock فعال است.
580. Secret Manager استفاده می‌شود.
581. پورت DB/Redis منتشر نمی‌شود.
582. RBAC صندوقی اعمال می‌شود.
583. SoD سازنده/تأییدکننده رعایت می‌شود.
584. 2FA فعال است.
585. WS احراز هویت‌شده است.
586. محدودیت اتصال و کلاینت کند مدیریت می‌شود.
587. توالی و snapshot پس از اتصال پشتیبانی می‌شود.
588. liveness از readiness جدا است.
589. تازگی داده در readiness لحاظ می‌شود.
590. Observability و متریک مستقر است.
591. هشدار خودکار تنظیم می‌شود.
592. NTP فعال است.
593. امنیت سایبری و برون‌سپاری کنترل می‌شود.
594. آزمون نفوذ انجام می‌شود.
595. مدیریت آسیب‌پذیری اجرا می‌شود.
596. رمزنگاری داده اعمال می‌شود.
597. حفاظت داده شخصی رعایت می‌شود.
598. دسترسی نظارتی لاگ می‌شود.
599. درگاه نظارتی read-only است.
600. معماری و ADR مستند می‌شود.

## ر) چرخه عمر — ۲۵ قلم

601. موافقت اصولی اخذ می‌شود.
602. ثبت نهاد مالی انجام می‌شود.
603. مجوز پذیره‌نویسی اخذ می‌شود.
604. حساب پذیره‌نویسی قفل است.
605. تکمیل پذیره‌نویسی و مجوز فعالیت اخذ می‌شود.
606. ارزش مبنای واحد تعیین می‌شود.
607. لغو پذیره‌نویسی و انحلال مدیریت می‌شود.
608. شروع فعالیت و راه‌اندازی کنترل می‌شود.
609. تمدید فعالیت و شرایط آن کنترل می‌شود.
610. افزایش سقف صندوق مدیریت می‌شود.
611. تغییر امیدنامه ثبت می‌شود.
612. تغییر مدیر/متولی ثبت می‌شود.
613. ادغام و ترکیب مدیریت می‌شود.
614. تبدیل نوع صندوق مدیریت می‌شود.
615. انحلال و تصفیه مدیریت می‌شود.
616. توزیع نهایی دارایی انجام می‌شود.
617. مالیات ادغام/انحلال لحاظ می‌شود.
618. گزارش انحلال تهیه می‌شود.
619. سابقه عمر صندوق نگهداری می‌شود.
620. آرشیو اسناد پس از انحلال حفظ می‌شود.
621. اهرمی: کف/سقف و تخصیص کنترل می‌شود.
622. اهرمی: سقف ۲ برابری کنترل می‌شود.
623. اهرمی: حداقل ۷۰٪ سهام کنترل می‌شود.
624. اهرمی: هزینه بهره محاسبه می‌شود.
625. تضمین: دو ساختار تفکیک می‌شود.

## ش) انواع ویژه — ۳۵ قلم

626. تضمین: دوره ۹۰ روز کنترل می‌شود.
627. تضمین: FIFO اعمال می‌شود.
628. تضمین: ذخیره ضامن نگهداری می‌شود.
629. تضمین: جریمه تأخیر محاسبه می‌شود.
630. تضمین: نسبت ۴ برابری کنترل می‌شود.
631. FOF: حداقل ۸۵٪ کنترل می‌شود.
632. FOF: سقف‌های نصاب کنترل می‌شود.
633. FOF: حلقه حلقوی کشف می‌شود.
634. FOF: کارمزد مضاعف تحلیل می‌شود.
635. جسورانه: تعهد سرمایه ثبت می‌شود.
636. جسورانه: Capital Call مدیریت می‌شود.
637. جسورانه: عمر ۷ سال کنترل می‌شود.
638. جسورانه: حداقل سرمایه‌گذاری کنترل می‌شود.
639. جسورانه: فرم پذیرش ریسک اخذ می‌شود.
640. املاک: آیین‌نامه ۱۴۰۴ رعایت می‌شود.
641. املاک: اموال با سند تک‌برگ و پایان‌کار کنترل می‌شود.
642. املاک: ارزیاب و انتقال اموال ثبت می‌شود.
643. املاک: واحد ممتاز/عادی دولت مدیریت می‌شود.
644. املاک: معافیت جهش تولید مسکن اعمال می‌شود.
645. ارزی: نرخ مرجع اعمال می‌شود.
646. ارزی: سپرده وکالتی تفکیک می‌شود.
647. تضمین تسویه: ساختار فقهی رعایت می‌شود.
648. بازارگردانی: یک‌درهزار ارزش بازار کنترل می‌شود.
649. نیکوکاری: حداقل سرمایه کنترل می‌شود.
650. بخشی: نصاب موضوعی کنترل می‌شود.
651. مختص اوراق دولتی: نصاب خاص کنترل می‌شود.
652. نوع دوم درآمد ثابت: نصاب خاص کنترل می‌شود.
653. شاخصی: مدیریت غیرفعال کنترل می‌شود.
654. اختیار/پوشش ریسک: مدل مصوب اعمال می‌شود.
655. صندوق تضمین: کارگروه و صلاحیت کنترل می‌شود.
656. تملک اهرمی: اوراق مرابحه مبتنی بر سهام کنترل می‌شود.
657. سپرده وکالتی: شناسایی درآمد طبق ماهیت وکالت انجام می‌شود.
658. صندوق املاک دولتی: مجمع مؤسس و انتقال مرحله‌ای کنترل می‌شود.
659. صندوق املاک دولتی: معافیت بانک مرکزی برای واحدهای دولتی کنترل می‌شود.
660. کمیته فقهی: سابقه تأیید هر ابزار نگهداری می‌شود.

## ت) بازارگردانی و معامله — ۲۰ قلم

661. دامنه مظنه کنترل می‌شود.
662. حداقل سفارش انباشته کنترل می‌شود.
663. حداقل معاملات روزانه کنترل می‌شود.
664. ممنوعیت هم‌جهت با صف کنترل می‌شود.
665. حجم هر سمت ≥ ۵۰٪ کنترل می‌شود.
666. قیمت در دامنه مجاز کنترل می‌شود.
667. بازارگردانی حراج/مذاکره تفکیک می‌شود.
668. ضمانت اسمی اوراق کنترل می‌شود.
669. موجودی بازارگردان پایش می‌شود.
670. گزارش تعهدات بازارگردان تولید می‌شود.
671. بازارگردانی الگوریتمی و کنترل منابع کنترل می‌شود.
672. رزرو اتمیک نقد/دارایی انجام می‌شود.
673. سفارش تکراری ممنوع است.
674. کلید توقف اضطراری فعال است.
675. تطبیق با کارگزار پس از قطعی انجام می‌شود.
676. قواعد نسخه‌دار نماد استفاده می‌شود.
677. TWAP/Iceberg با مدیریت اجرا اجرا می‌شود.
678. Best Execution در اجرا رعایت می‌شود.
679. سوابق سفارش و اجرا نگهداری می‌شود.
680. گزارش اجرای معاملات تولید می‌شود.

## ث) عملیات روزانه — ۲۵ قلم

681. چرخه T-1 و snapshot پایانی اجرا می‌شود.
682. آماده‌سازی پیش‌بازار انجام می‌شود.
683. قیمت مرجع روز محاسبه می‌شود.
684. تقویم و تعطیلی کنترل می‌شود.
685. NAV لحظه‌ای ریزمحاسبه می‌شود.
686. تطبیق دوره‌ای در جلسه انجام می‌شود.
687. هشدار حباب/واگرایی داده می‌شود.
688. کنترل کیفیت دائمی اجرا می‌شود.
689. NAV پایانی پس از بسته محاسبه می‌شود.
690. تطبیق رسمی روز انجام می‌شود.
691. نسخه روز بسته می‌شود.
692. گزارش مغایرت روز تولید می‌شود.
693. توزیع سود دوره‌ای انجام می‌شود.
694. آماده‌سازی چرخه فردا انجام می‌شود.
695. چک‌لیست عملیات روز تکمیل می‌شود.
696. تحویل شیفت و ثبت وقایع انجام می‌شود.
697. گزارش روزانه به مدیر ارائه می‌شود.
698. خطای عملیاتی کنترل و ثبت می‌شود.
699. تیکت عملیات ثبت و پیگیری می‌شود.
700. فید و اتصال پایش می‌شود.
701. صف و backlog پایش می‌شود.
702. تازگی داده پایش می‌شود.
703. تعهدات پایش می‌شود.
704. انطباق روز پایش می‌شود.
705. مدارک روز بایگانی می‌شود.

---

## نگاشت رجیستر به معماری

| حوزه رجیستر | ماژول متناظر در `NAV_ARCHITECTURE_V5.md` |
|---|---|
| الف | Source Adapters + Data Quality & Lineage |
| ب | Instrument Master + Fund Registry |
| ج | Portfolio Ledger + Position & Cash |
| د | Pricing + Valuation Engine |
| هـ | NAV Engine |
| و | Reconciliation + Trustee Panel |
| ز | Unit Registry / TA |
| ح | Corporate Actions |
| ط | Compliance + Prospectus Assembly |
| ی | AML/CFT |
| ک | Tax |
| ل | Governance |
| م | Fair Allocation + Personal Trading |
| ن | Complaint & Legal + Disciplinary + Eligibility |
| س | Risk & Analytics |
| ع | Performance & Rating |
| ف | Disclosure & Reporting + Regulator Portal |
| ص | Event/Integration |
| ق | زیرساخت و امنیت |
| ر | Fund Lifecycle + Merger & Liquidation |
| ش | ماژول‌های ویژه (Guarantee، FOF، ...) + Sharia Compliance |
| ت | Market Making + Execution |
| ث | عملیات روزانه (Cross-cutting) |
